---
title: Stack
date_created:
  "{ date:YYYY-MM-DD }":
---

## 🎯 Суть (Зачем нужно?)
> **Главная цель:** Какую проблему это решает?

Stack используется для реализации концепции LIFO (Last In First Out). Он необходим для управления процессами, где важна строгая последовательность операций отмены или возврата к предыдущему состоянию. Без стека не обойтись в:
- Рекурсия: Отслеживание состояния функций (стек вызовов)
- Отмена/Повтор: Сохранение истории действий пользователя
- Разбор выражений: Преобразование инфиксной записи в постфиксную
- Алгоритмы обхода: обход графов или деревьев в глубину

---

## 🧐 Механизм (Что это?)
> **Определение:** Стек - линейная структура данных, которая позволяет добавлять и удалять элементы только с одного конца, называемого вершиной (Top)
1.  **Основной принцип:** Элемент, который был добавлен последним - будет удален первым. push() - добавление элемента на вершину стека, pop() - удаление элемента с вершины и его возвращение, peek() - возвращение элемента с вершины без его удаления
2.  **Связь:** Stack является наследником класса [[Vector]] (который реализует интерфейс [[List (Список)]])


---

## 💻 Код

```java
import java.util.Stack;
// Название_функции или класса
public void simpleExample() {
    // Вставьте здесь чистый, минимальный код.
    Stack<String> nums = new Stack();
    nums.push("one");
    nums.push("two");
    nums.push("three");
    
    nums.peek() //three
    nums.pop() // удаление и возврат three, вершина теперь two
}
```


## 🧠 Заметки

- **Важно:** Класс java.util.Stack считается устаревшим и неэффективным из-за своей архитектуры и избыточной синхронизации. Для реализации Stack рекомендуется использовать [[ArrayDeque]] или [[LinkedList]] через интерфейс [[Deque]] 

## Ссылки
- **Ссылки**: 