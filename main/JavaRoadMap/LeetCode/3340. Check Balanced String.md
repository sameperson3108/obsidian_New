Для создания из строки чисел чисел в массив чисел

	...(String num)
	String[] numArr = num.splint("");
	int numbers[] = new int[numArr.length]
	for (int i = 0; i < numArr.length; i++) {
		numbers[i] = Integer.parseInt(numArr[i]);
	}